/**
* Architect.js is a build script.
* Features are :
* -> dynamic Elastic Beans config builder
* -> create a dist directory at the root
* -> launches the server build
* -> launches the client build
* -> launches the android / iphone build if there are any
*/
